import React, { Component } from 'react';
import {
  Text,
  View,
} from 'react-native';

import ListProducts from './ListProducts';

class App extends Component {
  
  render() {
      return (
        <ListProducts />
      );
    }
}

export default App;

