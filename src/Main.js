import React, { Component } from 'react';
import {
  Text,
  View,
} from 'react-native';

import ListProducts from './ListProducts';

class Main extends Component {
  
  render() {
      return (
        <ListProducts />
      );
    }
}

export default Main;

